<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
    <h1>Submitted Info</h1>
    <br/>
    <div>

        <div>@ViewBag.YourName</div>
        <div>@ViewBag.DojoLocation</div>
        <div>@ViewBag.FavoriteLanguage</div>
        <div>@ViewBag.CommentOptional</div>



@* <<<OR>>> *@

        @* @foreach(FormData s in Form)
        {
            <h2>@s.YourName</h2>
            <p>@s.DojoLocation</p>
            <p>@s.FavoriteLanguage</p>
            <p>@s.CommentOptional</p>
        } *@

@* <<<OR>>> *@
 
        @* <p>Name: @ViewBag.YourName</p>
        <p>Dojo Location: @ViewBag.DojoLocation</p>
        <p>Favorite Language: @ViewBag.FavoriteLanguage</p>
        <p>Comment (optional): @ViewBag.CommentOptional</p> *@
    </div>
    <br/>
    <button><a href="/">Go back</a></button>
    </div>
</body>
</html>